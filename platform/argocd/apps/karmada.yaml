apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: karmada
  namespace: argocd
spec:
  project: platform
  source:
    repoURL: https://github.com/Liberty911/multicloud-k8s-platform
    targetRevision: main
    path: platform/karmada
  destination:
    server: https://kubernetes.default.svc
    namespace: karmada-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
